let names = ['Ana', 'ana', 'John', 'john'];

console.log(names.sort((a, b) => a.localeCompare(b))); //retorna um número que indica se uma string de referência vem antes ou depois, ou é a mesma que a string fornecida na ordem de classificação.
